export enum HouseType {
  HOUSE = 'house',
  APT = 'apartment',
}

export enum PlaceType {
  ENTIRE = 'entire',
  PRV = 'private',
  SHARED = 'shared',
}

export enum WhoElse {
  ME = 'me',
  FAM = 'family',
  GUESTS = 'guests',
  ROOMMATES = 'roommates',
}

export enum PostStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  CLOSED = 'closed',
}
